version: '3.8'

services:
  ucs503-api:
    image: ghcr.io/<GITHUB_OWNER>/ucs503-api:latest
    restart: always
    env_file:
      - .env
    ports:
      - "8080:8080"
    # If you prefer to proxy via nginx on the host, expose only on 127.0.0.1:8080
    # ports:
    #   - "127.0.0.1:8080:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"

# optional: nginx service example (if you want to run proxy in compose)
#  nginx:
#    image: nginx:stable
#    volumes:
#      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
#    ports:
#      - "80:80"
#      - "443:443"
#    depends_on:
#      - ucs503-api
